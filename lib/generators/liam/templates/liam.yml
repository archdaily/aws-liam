default: &default
  skip: false
  aws:
    region:
    access_key_id:
    secret_access_key:
    sns:
      endpoint:
    sqs:
      endpoint:
      queue:
  events:

development: *default
test: *default
production: *default
