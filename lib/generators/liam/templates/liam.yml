default: &default
  aws:
    region:
    access_key_id:
    secret_access_key:
    sns:
      endpoint:
    sqs:
      endpoint:
      queue:
  topics:

development: *default
test: *default
production: *default
